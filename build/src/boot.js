export const boot = () => {
    //Recovery Mode
    //listen for f8 key
    document.addEventListener('keydown', function (e) {
        if (e.keyCode == 119) {
            //f8 key pressed
            //load recovery mode
            //TODO: add recovery mode
        }
    });
    
};